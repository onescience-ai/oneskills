# FourCastNet Global Region Hybrid Confirm

请先读取：

- `./oneskills/codex/case/second_round_guide.md`
- `./oneskills/codex/task/SKILL.md`

再继续本任务。

我已经确认你刚才输出的“详细执行信息”。
相关需要我确认的事项回复如下：
1. 区域尺寸：这部分通过多个参数确定，首先是图片尺寸 `region_img_size`，其次是经度范围 `region_lon_range`，最后是纬度范围 `region_lat_range`，输入范围的作用是让你能够更好的固定全球相关区域，或者你有其他的操作方式也可以。
2. 融合方式：将其作为单独的模块，目前先拼接后通过ConV或者MLP来融合。
3. 位置编码参考原来的的编码方式，因为我可能使用局部高精度分辨率，所以你需要考虑全球特征如何对齐，可以通过裁切全球特征，也可以通过压缩特征；
4. 你需要写一个虚拟数据生成（包含全球0.25度数据和局部数据，局部数据先采取0.01度数据，经度和纬度的范围都使用10度）、基础版全球权重快速获得（也就是写一个基座模型的快速训练中断方案）。

请基于确认后的方案继续生成完整代码。

如果你发现仍然存在会导致代码无法实现的关键冲突，请先简短指出；否则直接开始生成代码。

如果生成文件，请默认保存在当前 case 目录下。
