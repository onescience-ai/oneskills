# FourCastNet Global Region Hybrid Query

请先读取：

- `./oneskills/codex/case/first_round_guide.md`
- `./oneskills/codex/task/SKILL.md`

再处理我的任务。

基于开源模型构建一个全球+区域的天气预报系统。使用 FourCastNet 官方开源权重中最后一层的全球特征，同时，提供局部区域的气象数据及相关经纬度信息进行区域映射，训练一个能够融合全球特征的FourCastNet模型进行高分辨率预测。

我现在的想法是：构建一个全新的FourCastNet来训练一个局部模型，同时，提取开源权重的最后一层特征，在新的模型中的某部分进行融合。我会在新的模型中输入一个额外的参数就是地理位置范围，用于区域匹配。

我更希望先保持这些部分尽量不变：

- 输入输出形式先尽量保持和 `FourCastNet` 一样，除了部分多出来的变量
- 先做一个结构清晰、方便后续继续改的版本

我目前最关心的是：

- 这种改法在现有 `OneScience` 代码里是否合理、是否可实现
- 如果不能直接替换，最小改造路径应该是什么
- 哪些部分可以直接复用，哪些地方可能需要加一层桥接
- 希望尽量少改和这次任务无关的文件

我只看过论文，对具体源码实现不熟，所以请你先帮我把这件事翻译成一份清晰的实现方案，再决定后面怎么写代码。

如果后续进入代码生成阶段，请默认将最终代码文件保存在当前 case 目录下。
