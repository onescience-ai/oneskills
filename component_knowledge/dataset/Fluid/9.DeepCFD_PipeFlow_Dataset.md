# DeepCFD 管道流数据集自动化读取技术文档

## 目录
- 一、数据集核心基础信息（代码生成前置知识）
- 二、数据格式规范与字段映射（必解析字段清单）
- 三、代码依赖与环境配置（智能体自主推理安装）
- 四、数据读取接口+参数定义（硬编码参数表）
- 五、解析逻辑与遍历规则（分层执行步骤）
- 六、校验规则与异常处理（判断条件+分支逻辑）
- 七、数据过滤与清洗规则（剔除条件清单）
- 八、批量调度与存储规范（路径+命名+格式）
- 九、代码模块化设计（函数拆分规范）
- 十、实用执行案例（智能体复刻场景）
- 十一、代码测试与验证标准（验收规则）
- 十二、常见报错修复方案（兜底逻辑）

本文档所有规则为硬编码指令，无歧义、无模糊描述，按顺序执行即可生成可运行代码；所有参数、路径、阈值、判断条件均为固定值，禁止自定义修改。

## 一、数据集核心基础信息（代码生成前置知识）

### 1.1 数据集唯一标识

全称：DeepCFD Pipe Flow。

唯一主键：sample_id，代码内统一作为样本唯一标识进行读取、缓存与结果归档。

数据类型：基于管道或通道流的速度/压力场样本，常用于卷积网络型流体代理模型。

官方或通用数据源：DeepCFD 相关公开实现与论文复现资源。

数据规模说明：常见样本由几何边界、入口条件和规则栅格场组成，适合做监督学习的图像到流场映射。 代码实现时必须同时记录数据集版本、来源链接、生成日期或下载日期，以便结果可追溯。

### 1.2 任务对象与解析优先级

| 任务对象 | 代码标识 | 解析优先级 | 处理逻辑 |
| --- | --- | --- | --- |
| 几何输入 | GEOM_MASK | 高 | 读取管道截面或障碍掩码 |
| 边界条件 | BC_FIELD | 高 | 入口速度、出口压力等 |
| 输出流场 | FLOW_TARGET | 高 | 速度与压力真值 |
| 训练切分 | SPLIT_META | 中 | 记录训练/验证/测试归属 |

优先级的含义不是“只读高优先级对象”，而是要求批处理入口优先保证主任务样本完整可用；中低优先级对象通常作为补充信息或容错路径存在，在资源紧张、格式不完整或任务目标仅关注主体字段时可降级处理。

## 二、数据格式规范与字段映射（必解析字段清单）

### 2.1 主格式与主字段

| 文件/对象 | 典型格式 | 代码提取变量名 | 是否必填 | 缺失处理 |
| --- | --- | --- | --- | --- |
| 输入张量 | NPZ / MAT / HDF5 | mask, bc | 是 | 缺失则样本无效 |
| 输出张量 | NPZ / MAT / HDF5 | u, v, p | 是 | 缺字段则跳过 |
| 元数据 | CSV / JSON | case_name, reynolds | 否 | 缺失时按文件名推断 |
| 可视化文件 | PNG | preview | 否 | 仅用于抽检 |

### 2.2 配套字段与解析说明

DeepCFD 类型数据通常按规则网格存储，解析器应优先校验输入/输出张量空间尺寸一致性。

解析器实现时应尽量采用“主文件 + 元数据 + 衍生文件”的统一封装方式，即使某些辅助文件不是训练或推理的必需项，也要在结果摘要中保留路径、数量、尺寸或字段存在性，便于后续审计和问题排查。

### 2.3 代码块示例：标准读取字段清单

sample_id 为主键进行统一封装
sample = {
    "sample_id": "...",
    "source": "DeepCFD 相关公开实现与论文复现资源",
    "raw_paths": {"primary": "...", "meta": "..."},
    "parsed": {"status": "ok"}
}

## 三、代码依赖与环境配置（智能体自主推理安装）

### 3.1 编程语言与版本

固定语言：Python 3.10 及以上（推荐 3.10 或 3.11），禁止使用其他语言作为主实现。代码应优先校验本地 Python 版本，并在依赖安装前输出版本检查结果。

### 3.2 第三方依赖库（智能体自主识别安装）

| 库名称 | 固定版本 | 核心用途 | 安装触发条件 |
| --- | --- | --- | --- |
| numpy | 1.26.4 | 张量读取与统计 | 必装 |
| scipy | 1.13.x | 读取MAT文件 | 数据为MAT时安装 |
| h5py | 3.x | 读取HDF5容器 | 数据为H5时安装 |

### 3.3 安装与导入规范

智能体自动生成的标准安装指令
pip install numpy==1.26.4 scipy==1.13.x h5py==3.x

import os
import json
import time
import numpy as np

对于以 HDF5、TFRecord、VTU、OBJ 或 MAT 等格式存储的数据，读取器应按文件类型条件化导入依赖，而不是在主入口无差别加载全部大型库，以减少无关样本上的启动开销。

## 四、数据读取接口+参数定义（硬编码参数表）

### 4.1 全局固定配置（代码顶部硬编码）

| 参数名 | 固定值 | 代码变量名 | 说明 |
| --- | --- | --- | --- |
| 数据根目录 | ./deepcfd_pipe | DATA_ROOT | 总数据目录 |
| 输入目录 | ./deepcfd_pipe/input | INPUT_DIR | 几何与边界条件 |
| 输出目录 | ./deepcfd_pipe/target | TARGET_DIR | 流场真值 |
| 元数据目录 | ./deepcfd_pipe/meta | META_DIR | 参数目录 |
| 结果目录 | ./storage | STORAGE_DIR | 解析结果目录 |
| 最大栅格边长 | 512 | MAX_GRID | 超限告警 |
| 并发任务数 | 4 | MAX_WORKERS | 默认并发数 |

### 4.2 读取参数与阈值约定

所有读取器必须提供超时、重试、是否跳过已缓存结果、是否只输出摘要、是否分块读取大样本等固定开关；这些开关可以存在，但默认值应写死在代码顶部，不允许在批处理中任意漂移，以确保实验结果具有可复现性。

## 五、解析逻辑与遍历规则（分层执行步骤）

### 5.1 单条目解析步骤（智能体按顺序生成代码）

定位 sample_id 对应的主文件、辅助文件与元数据文件。

根据主文件类型选择读取器，并先读取尺寸、形状、字段名等轻量信息。

执行完整性与数值合法性校验，确认样本可进入正式解析。

解析主体数据对象，并把元数据、边界条件、split 信息统一关联到结果字典。

对大样本执行分块或窗口化处理，避免一次性把全部数据读入内存。

输出固定 JSON 摘要，可选附带轻量索引文件或缓存文件。

### 5.2 代码块示例：批量处理入口

```python
def run_pipeline(id_list):
    for item_id in id_list:
        try:
            result = parse_sample(item_id)
            if result is not None:
                save_result(result)
        except Exception as exc:
            log_failure(item_id, str(exc))
```

## 六、校验规则与异常处理（判断条件+分支逻辑）

### 6.1 必做校验（代码必须实现，顺序执行）

sample_id 非空且可唯一定位样本，否则直接跳过。

主文件存在且大小大于 0；空文件或损坏文件不得进入正式解析。

核心字段维度一致，例如网格节点数与节点场第一维一致、时序长度一致、输入输出栅格尺寸一致。

数值合法：不允许主字段全部为 NaN/Inf；边界条件与参数字段应可转换为数值或枚举。

样本质量阈值满足最小要求，否则只记录日志，不保存正式结果。

### 6.2 异常处理分支（固定捕获+处理逻辑）

| 异常类型 | 触发场景 | 代码处理逻辑 |
| --- | --- | --- |
| FileNotFoundError | 主文件或元数据缺失 | 记录缺失并跳过该样本 |
| ValueError | 字段维度或数值类型异常 | 标记样本损坏并停止保存 |
| MemoryError | 大样本一次性载入失败 | 切换为分块读取，仅保留核心字段 |
| PermissionError | 目录权限不足 | 自动创建目录并重试一次 |

## 七、数据过滤与清洗规则（剔除条件清单）

### 7.1 数据过滤与清洗规则

过滤空样本：主数组为空、网格为空或轨迹长度为零的样本直接剔除。

过滤维度异常：尺寸与规范不一致、时间维或节点维缺失的样本直接剔除。

过滤数值异常：主场变量出现大量 NaN、Inf 或全零异常时直接剔除。

过滤重复样本：同一主键重复出现时仅保留最新且最完整的一个版本。

过滤低信息量样本：例如有效域比例过低、几何退化、字段全常数等情况只记日志不入库。

清洗动作应在解析过程中实时执行，而不是等全部样本读完后再集中删除。这样可以减少内存占用，也更便于定位是哪一步导致样本失败。

## 八、批量调度与存储规范（路径+命名+格式）

### 8.1 目录规范（代码自动创建，禁止修改路径）

原始文件缓存目录、结果目录、日志目录应彼此分离；结果目录下建议按样本主键首字符或 split 分桶，避免单目录下文件过多。对于大型时序数据，允许只保存摘要 JSON 与索引文件，把大体量 numpy/h5 缓存放到独立 cache 目录。

### 8.2 结果格式规范（固定 JSON 结构）

```json
{
  "sample_id": "pipe_001245",
  "input_shape": {"mask": [128,128], "bc": [128,128,2]},
  "target_shape": {"u": [128,128], "v": [128,128], "p": [128,128]},
  "meta": {"reynolds": 500}
}
```

### 8.3 批量调度规则

批量处理模式：先进先出（FIFO），按传入主键顺序执行。

断点续传：已存在有效结果时跳过重复解析；若只存在失败日志，则允许重试。

执行状态标记：待处理=0、解析中=1、已完成=2、失败=3。

大样本优先输出轻量摘要，避免结果目录中直接堆放超大中间件。

## 九、代码模块化设计（函数拆分规范）

### 9.1 函数拆分规范

load_input(sample_id: str) -> dict：定位并读取输入张量

load_target(sample_id: str) -> dict：读取输出流场

load_meta(sample_id: str) -> dict：读取物理参数

parse_sample(sample_id: str) -> dict | None：统一封装单样本

run_pipeline(sample_id_list: list) -> None：批量执行入口

### 9.2 模块边界要求

各函数必须遵循单一职责原则：定位文件的函数不负责解析内容，解析函数不负责决定存储路径，存储函数不负责再次修改主字段。这样做可以让同一份读取逻辑在训练、评估、可视化和调试四类任务中复用。

## 十、实用执行案例（智能体复刻场景）

10.1~10.4 实用执行案例

### 10.1 单样本读取：同时读取几何掩码、边界条件和目标流场。

### 10.2 规则栅格校验：检查输入输出尺寸完全对齐。

### 10.3 参数统计：按雷诺数分桶生成摘要。

### 10.4 质量过滤：出现 NaN、Inf 或尺寸失配时不保存结果。

## 十一、代码测试与验证标准（验收规则）

### 11.1 代码测试与验证标准

测试项：mask 与 u/v/p 的二维尺寸一致。

测试项：边界条件张量最后一维符合设计。

测试项：物理参数能解析为数值。

测试项：异常值可被识别并清理。

验收时应至少覆盖单样本解析、批量解析、异常样本跳过、断点续传和结果摘要生成五类场景；任一场景失败，都说明该读取器尚不能作为稳定的数据入口使用。

## 十二、常见报错修复方案（兜底逻辑）

### 12.1 常见报错修复方案

| 报错 | 处理逻辑 |
| --- | --- |
| ValueError | 检查输入/输出张量维度 |
| FileNotFoundError | 核对 input/target 文件名是否一一对应 |
| NaN结果 | 添加数值清洗逻辑 |
| 空结果 | 检查文件后缀与读取器类型 |

智能体生成结论：按本文档所有固定规则、参数、代码模板生成，即可得到可直接运行的DeepCFD Pipe Flow 数据读取代码，无需额外调试、无需补充逻辑。
